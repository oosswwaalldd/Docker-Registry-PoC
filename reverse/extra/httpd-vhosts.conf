<VirtualHost *:80>
  ProxyPreserveHost On
  ProxyRequests On
  ServerName ${SERVER_NAME}
  Redirect / https://${SERVER_NAME}/
  # ProxyPass / http://webserver:${WEBSERVER_PORT}/
  # ProxyPassReverse / http://webserver:${WEBSERVER_PORT}/
</VirtualHost>